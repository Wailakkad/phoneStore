/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.pinimg.com",
      // add other allowed domains here if needed
    ],
  },
};

module.exports = nextConfig;